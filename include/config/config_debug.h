#pragma once

// Serial Debug Enable
#if !defined(SERIAL_DEBUG_ENABLE)
#define SERIAL_DEBUG_ENABLE                 false
#endif

// Startup Debug Delay in milliseconds
#if !defined(STARTUP_DEBUG_DELAY)
#define STARTUP_DEBUG_DELAY                 0
#endif

